<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AlarmClock" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/lavendor</item>
         <item name="colorPrimaryVariant">@color/lavendor</item>
        <item name="android:statusBarColor">@color/lavendor</item>
    </style>

    <style name="Theme.AlarmClock" parent="Base.Theme.AlarmClock" />
</resources>